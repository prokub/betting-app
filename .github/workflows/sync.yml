name: Sync matches

on:
  schedule:
    - cron: "0 6 * * 3" # Every Wednesday at 6am UTC
    - cron: "0 6 * * 4" # Every Thursday at 6am UTC
    - cron: "0 17 * * 4" # Every Thursday at 5pm UTC (~1h before matches) to catch same-day cancellations
  workflow_dispatch: # Also allows manual trigger from GitHub UI

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Call sync endpoint
        run: |
          curl -X POST ${{ secrets.APP_URL }}/api/sync-matches \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
